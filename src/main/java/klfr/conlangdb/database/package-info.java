
/**
 * Classes that deal with database connectivity, accessing and operating on the
 * database. For external users the most important pieces are the
 * DatabaseCommunicator, which submits commands to the database, and the
 * DatabaseCommand subclasses, which represent atomic operations on the
 * database.
 */
package klfr.conlangdb.database;